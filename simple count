#include <stdio.h>

int main(void) {
	// your code goes here
	int n,k;
	scanf("%d%d",&n,&k);
	int arr[n],i,count=0;
	for(i=0;i<n;i++)
	{
	    scanf("%d",&arr[i]);
	}
	for(i=0;i<n;i++)
	{
	    if(arr[i]>=k)
	    count++;
	}
	printf("%d",count);
	return 0;
}
